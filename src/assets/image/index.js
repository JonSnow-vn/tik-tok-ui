const logo = {
    path: require("~/assets/image/logo.svg").default
};

const deleteIcon = {
    path: require("~/assets/image/delete.svg").default
};

const loadingIcon = {
    path: require("~/assets/image/loading.svg").default
};

const searchIcon = {
    path: require("~/assets/image/search.svg").default
};
export {logo, deleteIcon, loadingIcon, searchIcon};